import * as fs from 'fs';
import FolderAlredyExistsError from '../error/FolderAlredyExistsError.js';
import FolderNotExistsError from '../error/FolderNotExistsError.js';
export declare enum FileOpenModes {
    WriteOrOverwrite = 0,
    Append = 1
}
export default class IOOperationsHandler {
    private static error;
    static createFolder(folderCompleteName: fs.PathLike, force?: boolean, debug?: boolean): FolderAlredyExistsError | Error | void;
    static createFile(targetFolder: fs.PathLike, fileName: fs.PathLike, content: string, mode?: FileOpenModes, force?: boolean, debug?: boolean): FolderNotExistsError | void;
    private static defaultNoParamsCallBack;
    private static defaultCallBackError;
}
